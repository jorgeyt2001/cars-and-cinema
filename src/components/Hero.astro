---
---

<section class="relative min-h-screen flex items-center justify-center overflow-hidden pt-16 sm:pt-20 lg:pt-24 w-full digital-rain">
  <!-- Animated background gradient with enhanced effects -->
  <div class="absolute inset-0 z-0">
    <div class="absolute inset-0 bg-gradient-to-br from-[#d4af37]/20 via-transparent to-[#ff6b00]/20 animate-gradient"></div>
    <!-- Cyber grid overlay -->
    <div class="absolute inset-0 opacity-10" style="background-image: radial-gradient(circle, #d4af37 1px, transparent 1px); background-size: 30px 30px;"></div>
  </div>

  <div class="w-full flex justify-center z-10">
    <div class="text-center max-w-5xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="mb-6 sm:mb-8 opacity-0 animate-fade-in-up" style="animation-delay: 0.2s;">
        <span class="inline-block px-4 sm:px-6 py-2 glass holographic-border rounded-full text-[#d4af37] font-semibold text-xs sm:text-sm tracking-wider neon-pulse">
          LUXURY CAR RENTAL EXPERIENCE
        </span>
      </div>

      <h1 class="text-4xl sm:text-5xl md:text-6xl lg:text-7xl xl:text-9xl font-black mb-4 sm:mb-6 opacity-0 animate-fade-in-up leading-tight perspective-container" style="font-family: var(--font-heading); animation-delay: 0.4s;">
        <span class="holographic-text block mb-2 relative" data-text="CONDUCE">CONDUCE</span>
        <span class="neon-text block neon-pulse">TUS SUEÑOS</span>
      </h1>

      <p class="text-base sm:text-lg md:text-xl lg:text-2xl text-white/70 mb-8 sm:mb-10 lg:mb-12 max-w-3xl mx-auto opacity-0 animate-fade-in-up px-4" style="animation-delay: 0.6s;">
        Experimenta el lujo cinematográfico con nuestra exclusiva colección de vehículos premium.
        De las pantallas a las calles.
      </p>

      <div class="flex flex-col sm:flex-row gap-4 sm:gap-6 justify-center items-center opacity-0 animate-fade-in-up px-4" style="animation-delay: 0.8s;">
        <a
          href="#flota"
          class="group w-full sm:w-auto px-8 sm:px-10 py-4 sm:py-5 bg-[#d4af37] text-black font-bold rounded-lg text-base sm:text-lg hover:bg-white transition-smooth magnetic-button glow-gold text-center relative overflow-hidden shimmer cyber-glow"
        >
          <span class="relative z-10">EXPLORA LA FLOTA</span>
          <div class="absolute inset-0 bg-gradient-to-r from-transparent via-white/30 to-transparent translate-x-[-100%] group-hover:translate-x-[100%] transition-transform duration-1000"></div>
        </a>
        <a
          href="#reservar"
          class="group w-full sm:w-auto px-8 sm:px-10 py-4 sm:py-5 glass-enhanced border-2 border-[#d4af37]/50 text-white font-bold rounded-lg text-base sm:text-lg hover:border-[#d4af37] transition-smooth magnetic-button text-center relative overflow-hidden holographic-border"
        >
          <span class="relative z-10">VER DISPONIBILIDAD</span>
          <div class="absolute inset-0 bg-[#d4af37]/20 scale-0 group-hover:scale-100 transition-transform duration-500 rounded-lg"></div>
        </a>
      </div>

      <!-- Stats - ULTRA FUTURISTIC -->
      <div class="grid grid-cols-2 lg:grid-cols-4 gap-4 sm:gap-6 lg:gap-8 mt-12 sm:mt-16 lg:mt-20 opacity-0 animate-fade-in-up px-4" style="animation-delay: 1s;">
        <div class="glass-enhanced p-4 sm:p-6 rounded-xl hover-lift transform-3d-extreme gradient-border-animated group neon-lines energy-shield warp-speed float-animation">
          <div class="text-3xl sm:text-4xl font-bold mb-2 bg-gradient-to-r from-[#d4af37] to-[#ffd700] bg-clip-text text-transparent chromatic" style="font-family: var(--font-heading);">50+</div>
          <div class="text-white/60 text-xs sm:text-sm group-hover:text-white/90 transition-colors">Vehículos Premium</div>
        </div>
        <div class="glass-enhanced p-4 sm:p-6 rounded-xl hover-lift transform-3d-extreme gradient-border-animated group neon-lines energy-shield warp-speed float-animation" style="animation-delay: 0.2s;">
          <div class="text-3xl sm:text-4xl font-bold mb-2 bg-gradient-to-r from-[#d4af37] to-[#ffd700] bg-clip-text text-transparent chromatic" style="font-family: var(--font-heading);">24/7</div>
          <div class="text-white/60 text-xs sm:text-sm group-hover:text-white/90 transition-colors">Servicio al Cliente</div>
        </div>
        <div class="glass-enhanced p-4 sm:p-6 rounded-xl hover-lift transform-3d-extreme gradient-border-animated group neon-lines energy-shield warp-speed float-animation" style="animation-delay: 0.4s;">
          <div class="text-3xl sm:text-4xl font-bold mb-2 bg-gradient-to-r from-[#d4af37] to-[#ffd700] bg-clip-text text-transparent chromatic" style="font-family: var(--font-heading);">98%</div>
          <div class="text-white/60 text-xs sm:text-sm group-hover:text-white/90 transition-colors">Satisfacción</div>
        </div>
        <div class="glass-enhanced p-4 sm:p-6 rounded-xl hover-lift transform-3d-extreme gradient-border-animated group neon-lines energy-shield warp-speed float-animation" style="animation-delay: 0.6s;">
          <div class="text-3xl sm:text-4xl font-bold mb-2 bg-gradient-to-r from-[#d4af37] to-[#ffd700] bg-clip-text text-transparent chromatic" style="font-family: var(--font-heading);">15+</div>
          <div class="text-white/60 text-xs sm:text-sm group-hover:text-white/90 transition-colors">Años Experiencia</div>
        </div>
      </div>
    </div>
  </div>

  <!-- Scroll indicator -->
  <div class="absolute bottom-6 sm:bottom-10 left-1/2 transform -translate-x-1/2 opacity-0 animate-bounce" style="animation-delay: 1.5s;">
    <svg class="w-5 h-5 sm:w-6 sm:h-6 text-[#d4af37]" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3"></path>
    </svg>
  </div>
</section>

<style>
  @keyframes fade-in-up {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-fade-in-up {
    animation: fade-in-up 0.8s ease-out forwards;
  }

  @keyframes gradient {
    0%, 100% {
      transform: scale(1) rotate(0deg);
    }
    50% {
      transform: scale(1.2) rotate(5deg);
    }
  }

  .animate-gradient {
    animation: gradient 20s ease-in-out infinite;
  }

  @keyframes bounce {
    0%, 100% {
      transform: translateY(0);
      opacity: 1;
    }
    50% {
      transform: translateY(-20px);
      opacity: 0.5;
    }
  }

  .animate-bounce {
    animation: bounce 2s ease-in-out infinite;
  }
</style>
